; Increase the maximum number of input variables allowed per request.
; The default value of 1000 is too low for schools with many education entries
; (e.g. TSM Mechelen), causing PHP to silently truncate POST data and drop the
; CSRF token, resulting in a CSRF token validation error.
max_input_vars = 5000
